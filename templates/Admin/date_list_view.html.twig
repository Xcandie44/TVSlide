{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field%}

    <div style="text-align: center">
            <p>{{ object.showFrom|date('d.m.y') }} - {{ object.showFor|date('d.m.y') }}</p>

    </div>
{% endblock %}